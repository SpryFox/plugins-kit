# UE Python Script Runner — global defaults
#
# Project-specific paths (engine_dir, uproject) go in:
#   ~/.claude/plugins/data/unreal-kit/config.yaml
#
# Resolution order: CLI args → local project config → this file → hardcoded defaults

remote_execution:
  timeout_seconds: 5
  multicast_group: "239.0.0.1"
  multicast_port: 6766
