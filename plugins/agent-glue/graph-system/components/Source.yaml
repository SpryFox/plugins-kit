kind: Source
description: Edge source. Identifies the from-node and the output variant of that node's output contract that this edge fires on.
fields:
  from_node:
    type: string
    required: true
  on_variant:
    type: string
    required: true
    description: Name of one discriminated-union variant within from_node's output contract.
