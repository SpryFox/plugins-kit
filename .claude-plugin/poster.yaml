# Marketplace metadata for the plugin-ecosystem-poster skill
# (plugins-kit:awesome-kit plugin-ecosystem-poster).
#
# Presence of this file opts the marketplace into the poster.
# All fields are optional; omit any you do not want shown.

subtitle: "Christina's open source plugin repository"
url: "https://github.com/kitaekatt/plugins-kit"

# Marketplace-author state declarations. Keyed by short plugin name (no marketplace prefix).
# Values: on | off | opt-in | required.
# `required` = no other plugin in this marketplace works without it -- can't be turned off.
states:
  bootstrap: required
