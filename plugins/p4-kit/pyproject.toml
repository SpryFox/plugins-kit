[project]
name = "p4-kit"
version = "0.10.0"
description = "Host-side dependencies for p4-kit plugin"
requires-python = ">=3.12,!=3.14.*"

dependencies = [
    "bootstrap @ git+https://github.com/kitaekatt/plugins-kit.git#subdirectory=plugins/bootstrap",
]
