kind: Topology
description: Input and output contract names for a node. Both are resolved by the loader against the graph's contracts module.
fields:
  in:
    type: string
    required: true
    description: Name of the Pydantic input contract class.
  out:
    type: string
    required: true
    description: Name of the Pydantic output contract class (often a discriminated union).
