name: bad-both
description: A step may not declare both agent and pipeline.
steps:
  - id: x
    agent:
      prompt: "hi"
    pipeline:
      over: [a, b]
      as: item
      stages:
        - id: s
          agent:
            prompt: "yo {{ item }}"
