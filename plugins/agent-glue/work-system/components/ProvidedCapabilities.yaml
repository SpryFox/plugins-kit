kind: ProvidedCapabilities
description: Tools and MCP servers a Worker can satisfy in CapabilityRequirement intersections. Dynamic for workers whose set depends on the running environment (claude_agent intersects against discovered MCP servers at submit time).
fields:
  tools:
    type: list
    required: false
    items:
      type: string
  mcp_servers:
    type: list
    required: false
    items:
      type: string
