[project]
name = "p4-kit"
version = "0.1.0"
description = "Host-side dependencies for p4-kit"
requires-python = ">=3.11"

dependencies = [
    "code-review @ git+https://github.com/kitaekatt/code-review-research.git",
    "pyyaml",
]
